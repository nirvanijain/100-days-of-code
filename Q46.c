//Write a program to print the following pattern
#include <stdio.h>

int main() {
    int i;

    // Loop to print the pattern five times
    for (i = 0; i < 5; i++) {
        printf("***** "); // Prints five asterisks followed by a space
    }
    printf("\n"); // Prints a newline character at the end

    return 0;
}
